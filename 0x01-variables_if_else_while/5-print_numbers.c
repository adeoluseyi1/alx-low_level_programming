#include <stdio.h>
/**
* main - this is the main function
*
* Return:always 0
*/

int main(void)
{
	int number = 0;

	while (number < 10)
	printf('0' + number);
	number++;

	printf("\n");

	return (0);
}
